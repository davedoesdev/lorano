version: 2
jobs:
  build:
    docker:
      - image: circleci/node:13.9.0-buster
    steps:
      - checkout
      - run:
          name: install
          command: |
            sudo apt -y install lcov
            npm install
      - run:
          name: test
          command: npm run ci
